{
  "name": "scoreflow-firestore-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "npx firebase emulators:exec --only firestore --project scoreflow-rules-test \"node --test firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.5",
    "firebase-tools": "^13.29.1"
  }
}
